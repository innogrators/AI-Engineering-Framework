# As-is spec: <area name>

> Status: AS-IS (documented <date>)
> Documents OBSERVED current behavior, not intent. Never flip to APPROVED — target changes get their own spec via /write-spec.

## Observed behavior
<per flow: trigger -> actual behavior -> side effects; cite file:line where useful>

## Implicit invariants discovered
<rules the code relies on but nowhere states, e.g. "status only ever set in X"; candidates for CLAUDE.md domain invariants>

## Known quirks & bugs
| # | Behavior | Quirk or bug? | Keep / fix-candidate | Files |
| :-- | :-- | :-- | :-- | :-- |

## Open questions
<everything not determinable from code/tests/docs — for a human to answer>

## Affected files
